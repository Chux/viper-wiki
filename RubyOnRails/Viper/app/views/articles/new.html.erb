<article>
<%= notice %>
	<% 
		if @article.title == nil
			%><h1>Create new article</h1><%
		else
			%><h1>"<%= @article.title %>" does not exist</h1><%
			%><h2>Create it</h2><%
		end
	%>

	<%= render 'form' %>

	<%= link_to 'Back', articles_path %>
</article>
